package com.sf.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.sf.dbswitch.core.db.ormlite.SimpleData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SIMPLE_DATA".
*/
public class SimpleDataDao extends AbstractDao<SimpleData, Long> {

    public static final String TABLENAME = "SIMPLE_DATA";

    /**
     * Properties of entity SimpleData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property String = new Property(1, String.class, "string", false, "STRING");
        public final static Property Millis = new Property(2, Long.class, "millis", false, "MILLIS");
        public final static Property Date = new Property(3, java.util.Date.class, "date", false, "DATE");
        public final static Property Even = new Property(4, Boolean.class, "even", false, "EVEN");
    }


    public SimpleDataDao(DaoConfig config) {
        super(config);
    }
    
    public SimpleDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SIMPLE_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"STRING\" TEXT," + // 1: string
                "\"MILLIS\" INTEGER," + // 2: millis
                "\"DATE\" INTEGER," + // 3: date
                "\"EVEN\" INTEGER);"); // 4: even
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SIMPLE_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SimpleData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String string = entity.getString();
        if (string != null) {
            stmt.bindString(2, string);
        }
 
        Long millis = entity.getMillis();
        if (millis != null) {
            stmt.bindLong(3, millis);
        }
 
        java.util.Date date = entity.getDate();
        if (date != null) {
            stmt.bindLong(4, date.getTime());
        }
 
        Boolean even = entity.getEven();
        if (even != null) {
            stmt.bindLong(5, even ? 1L: 0L);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SimpleData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String string = entity.getString();
        if (string != null) {
            stmt.bindString(2, string);
        }
 
        Long millis = entity.getMillis();
        if (millis != null) {
            stmt.bindLong(3, millis);
        }
 
        java.util.Date date = entity.getDate();
        if (date != null) {
            stmt.bindLong(4, date.getTime());
        }
 
        Boolean even = entity.getEven();
        if (even != null) {
            stmt.bindLong(5, even ? 1L: 0L);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public SimpleData readEntity(Cursor cursor, int offset) {
        SimpleData entity = new SimpleData( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // string
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // millis
            cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)), // date
            cursor.isNull(offset + 4) ? null : cursor.getShort(offset + 4) != 0 // even
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SimpleData entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setString(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setMillis(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setDate(cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)));
        entity.setEven(cursor.isNull(offset + 4) ? null : cursor.getShort(offset + 4) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(SimpleData entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(SimpleData entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(SimpleData entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
